{
  "name": "parent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/examples",
    "packages/library"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -p -v -i run build",
    "clean": "yarn workspaces foreach run clean"
  }
}
