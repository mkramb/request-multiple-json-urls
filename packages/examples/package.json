{
  "name": "@ft/examples",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "run:simple": "ts-node ./src/simple.ts",
    "run:with-concurrency": "ts-node ./src/with-concurrency.ts",
    "run:with-failure": "ts-node ./src/with-failure.ts",
    "run:with-retry": "ts-node ./src/with-retry.ts"
  },
  "dependencies": {
    "@ft/library": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
